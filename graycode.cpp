#include<bits/stdc++.h>
#include<math.h>

using namespace std;

class gray{
    public:
int arr1[];
int j=0;
int group(int arr[]){
     for(int i=0; i<=x; i++){
        arr[i] = i;
    }
    convert(arr[]);

    return o;
}
int convert(int arr[]){
    for(int i=0; i<arr.size(); i++){
        binary(arr[i]);
    }
    sort();

    return 0;
}

int binary(int decimal){
  while (decimal != 0) {
    int remainder = decimal % 2;
    int binary = binary + (remainder * product);
    decimal = decimal / 2;
    int product *= 10;
}   
    arr1[j] = decimal;
    j++;

}
int sort(arr a){
    int l = arr1[0]

    for(int k=0; k<arr1.size(); k++){
        if(arr1[k]>l){
            l = arr1[k]
        }
        else
           continue;
    }
}
    for(int v=0; v<arr1.size(); v++){
    cout << arr1[v] << "";
    }

};



int main(){
    int n;
    cin >> n;
    x = (pow(2,n)-1);


    return 0;
}
